// import { configureStore } from '@reduxjs/toolkit'       // configureStore is used to      before this we have to import thunk, createStore etc. 
// import counterSlice from './counterSlices'              // Now counterSlice(function) is have to import from counterSlices.js

// export const store = configureStore({
//   reducer: {counter: counterSlice},                     // in reducer it is a wraper in this we can use multiple reducer
// })